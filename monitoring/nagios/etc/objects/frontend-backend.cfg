define host {
    use                     linux-server
    host_name               frontend
    alias                   Website
    address                 13.51.106.118
}

define service {
    use                     generic-service
    host_name               frontend
    service_description     HTTP Check
    check_command           check_http!-p 80
}


define host {
    use                     linux-server
    host_name               backend
    alias                   Backend API
    address                 13.51.106.118
}

define service {
    use                     generic-service
    host_name               backend
    service_description     Backend HTTP Check
    check_command           check_http!-p 5454 -u /
}

define service {
    use                     generic-service
    host_name               backend
    service_description     Backend Health Endpoint
    check_command           check_http!-p 5454 -u /health
}
